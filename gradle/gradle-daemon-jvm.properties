#This file is generated by updateDaemonJvm
# Detekt 1.23.8 embeds a Kotlin compiler that cannot run on JDK 25, and the project already
# compiles against Java 17 (see the java toolchain in app/build.gradle.kts) and runs its CI on
# JDK 17, so pin the daemon to the same version instead of whatever JVM launched the build.
toolchainVersion=17
